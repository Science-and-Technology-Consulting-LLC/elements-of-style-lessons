#' Step 3 — voom transformation.
#'
#' Apply the voom precision-weight transformation given a counts
#' matrix, a normalisation factor table, and a design matrix.
#'
#' PLACEHOLDER.
#'
#' @param counts  Filtered counts matrix from prep_counts.
#' @param design  Design matrix from make_design.
#' @return An EList object (or NULL placeholder).
#' @export
run_voom <- function(counts, design) {
  message("[limma-voom] run_voom placeholder")
  NULL
}
